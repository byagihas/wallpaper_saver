## Scripts to automate retrieving top wallpaper posts from r/wallpapers

This project uses the reddit API. 
You'll need to get a token from token.json after setting reddit API Auth parameters in a .env file. 
see npm package 'dotenv' for more details:

The script uses the following .env parameters:

REDDIT_API_SECRET=
REDDIT_API_CLIENT=
REDDIT_REDIRECT_URI=
REDDIT_USER=
REDDIT_PASS=

Once .env file has been set up, run:

node index.js

