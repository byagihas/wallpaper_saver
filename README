## Scripts to automate retrieving top wallpaper posts from r/wallpapers

You'll need to get a token from token.json after setting reddit Auth parameters in a .env file:

REDDIT_API_SECRET=
REDDIT_API_CLIENT=
REDDIT_REDIRECT_URI=
REDDIT_USER=
REDDIT_PASS=

Then run:

node index.js

